// const frutas = ['Pera', 'Uva', 'Abacate' ];
// for in = ler os índices ou chaves do objeto

/* 
for (let i = 0; i < frutas.length; i++) {
    console.log(frutas[i]);
} 
*/

/* 
for (let i in frutas) {
    console.log(frutas[i]);
} 
*/

// Criando um objeto pessoa
const pessoa = {
    nome: 'Guilherme',
    sobrenome: 'Marrafon',
    idade: 33
};

/* 
// Usando for in para percorrer as chaves do objeto
for (let keys in pessoa) {
    console.log(keys, pessoa[keys]);
} 
*/
